#include <iostream>
#include "01.4.h"
using namespace std;
using namespace AddProgram;
using namespace MutipleProgram;

int main(void)
{
    int a=10, b=5;
    cout<<"add(a , b) = "<<add(a,b)<<"\n";
    cout<<"multiple(a , b) = "<<multiple(a,b);
}